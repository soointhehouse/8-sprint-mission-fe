import React from "react";
import ReactDOM from "react-dom/client";
import "./index.css"; // 스타일 옮겨둔 파일
import App from "./App";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(<App />);